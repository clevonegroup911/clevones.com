# T043 — Pont télémétrie Fedora AUTOPILOT → /admin/x200

## Statut

TERMINÉE

## Objectif

Remplacer les stubs T042 (`NOT_CONNECTED` / `WAITING_FOR_TELEMETRY`) par un heartbeat réel écrit par le superviseur Fedora et lu serveur-side par le Control Center.

## Livré

- Writer `.x200/telemetry.json` (`scripts/lib/x200-telemetry.mjs`) mode `0600`, dossier `.x200` `0700`, sans secrets
- Autopilot publie BOOT / FAST_LANE / AUTOPLAN / WAIT / HUMAN_GATE / AUTOPLAN_COMPLETE / SHUTDOWN
- Reader `lib/x200/telemetry.ts` : absent → NOT_CONNECTED ; invalide → INVALID ; stale → STALE ; frais → OK + live state réel (jamais de faux RUNNING)
- Control Center consomme la télémétrie réelle (cartes ACTIVE AGENT / blockers)
- Docs `docs/X200_AUTOPILOT.md` mises à jour
- CI #112 recovery : `readJsonBody` → 400 déterministe pour JSON vide/malformé ; e2e payments via `fetch` navigateur

## Contrôles locaux

- validate / unit / x200:test / lint / tsc / build / secrets / diff-check : PASS

## CI

- FULL quality SUCCESS run `34763250287` on `931727619b396319a5c093ccd5ec51142b261d06`
- https://github.com/clevonegroup911/clevones.com/actions/runs/34763250287
- `mode=FULL` `ok=true` ; Playwright exit 0 ; failed=none
- MERGED=NO ; DEPLOYED=NO
