/** Routes where the fixed mobile CTA bar should be hidden. */
export const MOBILE_CTA_HIDDEN_PATHS = new Set([
  "/contact",
  "/sign-in",
  "/portal",
  "/confidentialite",
  "/mentions-legales",
]);
